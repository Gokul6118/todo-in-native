{
  "version": 2,
  "buildCommand": "pnpm --filter @repo/server build",
  "outputDirectory": "packages/server/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/index.js" },
    { "source": "/(.*)", "destination": "/index.js" }
  ]
}